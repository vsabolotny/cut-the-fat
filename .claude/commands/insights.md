KI-Sparempfehlungen anzeigen mit dem Cut the Fat CLI.

Führe aus:
```bash
cd . . &&. && ./ctf insights $ARGUMENTS
```

Zeige die Ausgabe. Mit `--neu` werden die Empfehlungen neu generiert (Cache wird ignoriert).
