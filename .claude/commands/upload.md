Kontoauszug importieren mit dem Cut the Fat CLI.

Führe aus:
```bash
cd /Users/ecog-vladislav/Projects/cut-the-fat && ./ctf upload $ARGUMENTS
```

Zeige die Ausgabe. Falls kein Dateiname angegeben wurde, weise darauf hin dass ein Pfad zu einer CSV-, Excel- oder PDF-Datei benötigt wird.
